import { GraduationCap } from 'lucide-react';

const Logo = () => {
  return (
    <div className="flex items-center justify-center w-80 h-80 md:w-120 md:h-120 bg-primary rounded-xl">
                <GraduationCap className="w-60 h-60 md:w-80 md:h-80 text-primary-foreground" />
    </div>
  );  
};

export default Logo; 